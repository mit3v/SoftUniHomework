function cityRecord(a, b, c) {

    let city = {};
    city.name = a;
    city.population = b;
    city.treasury = c;
    console.log(city);
    // return city;

}

cityRecord('Tortuga', 7000, 15000);
cityRecord('Santo Domingo', 12000, 23500);